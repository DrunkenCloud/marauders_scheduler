import EntityContent from '@/components/EntityContent'
import { EntityType } from '@/types'

export default function ScheduleCoursesPage() {
  return <EntityContent entityType={EntityType.SCHEDULE_COURSES} />
}
