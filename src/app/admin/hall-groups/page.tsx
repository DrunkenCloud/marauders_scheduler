import EntityContent from '@/components/EntityContent'
import { EntityType } from '@/types'

export default function HallGroupsPage() {
  return <EntityContent entityType={EntityType.HALL_GROUP} />
}
