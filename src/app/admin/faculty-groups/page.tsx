import EntityContent from '@/components/EntityContent'
import { EntityType } from '@/types'

export default function FacultyGroupsPage() {
  return <EntityContent entityType={EntityType.FACULTY_GROUP} />
}
