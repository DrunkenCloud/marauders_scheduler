import EntityContent from '@/components/EntityContent'
import { EntityType } from '@/types'

export default function StudentGroupsPage() {
  return <EntityContent entityType={EntityType.STUDENT_GROUP} />
}
