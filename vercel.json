{
  "buildCommand": "pnpx prisma generate && pnpx prisma migrate deploy && next build && npm run db:seed",
  "installCommand": "pnpm install && pnpx prisma generate",
  "env": {
    "PRISMA_GENERATE_SKIP_AUTOINSTALL": "true"
  }
}